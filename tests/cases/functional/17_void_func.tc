int acc = 0;

void add(int x) {
    if (x < 0) {
        return;
    }
    acc = acc + x;
}

void addRange(int n) {
    int i = 1;
    while (i <= n) {
        add(i);
        i = i + 1;
    }
}

int main() {
    add(-5);
    addRange(10);
    return acc;
}
